import { Typography, Container } from '@mui/material'

const Home = () => {
  return (<>
    <Container>
      <Typography variant="h4">Home</Typography>
    </Container>
  </>)
};

export default Home